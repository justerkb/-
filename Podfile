# Uncomment the next line to define a global platform for your project
# platform :ios, '17.0'

target 'HalykBank' do
  use_frameworks!
	
	pod 'FirebaseCore' 
	pod 'FirebaseAuth'
	pod 'FirebaseFirestore' 
	pod 'FirebaseStorage'

end
